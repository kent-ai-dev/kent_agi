# python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# experiment artifacts (committed samples live under results/)
checkpoints/
checkpoints*/
results*.json

# model checkpoints, packed token data, chat logs — these live in Modal
# volumes and may get pulled into the working tree; never commit them.
*.pt
*.bin
chat_logs.jsonl
*.bak

# os
.DS_Store
